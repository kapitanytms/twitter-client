import { JustTweetedDirective } from './just-tweeted.directive';

describe('JustTweetedDirective', () => {
  it('should create an instance', () => {
    const directive = new JustTweetedDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
